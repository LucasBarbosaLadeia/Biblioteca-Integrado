import { Router } from "express";
import { EmprestimoController } from "../controller";

const router = Router();

// Rotas para Empréstimos
// router.get("/", EmprestimoController.getAll);
// router.get("/ativos", EmprestimoController.getAtivos);
// router.get("/atrasados", EmprestimoController.getAtrasados);
// router.get("/usuario/:usuarioId", EmprestimoController.getByUsuario);
// router.get("/estatisticas", EmprestimoController.getEstatisticas);
// router.get("/:id", EmprestimoController.getById);
// router.post("/", EmprestimoController.create);
// router.put("/:id/devolver", EmprestimoController.devolver);
// router.put("/:id/renovar", EmprestimoController.renovar);
// router.delete("/:id", EmprestimoController.delete);

export default router;
